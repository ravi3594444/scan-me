# ui/shared

Compose Multiplatform UI shared by the Android and desktop apps: tokens and theme (design §1), radar, bubbles,
drop animation, cards, tray and the dashboard tabs (design §3–6). Screens take plain state objects; the app modules
map engine `StateFlow`s to them. Built out in WP8.
